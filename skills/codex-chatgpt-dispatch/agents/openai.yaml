interface:
  display_name: "Codex ChatGPT Dispatch"
  short_description: "整理材料并单次调度 ChatGPT"
  default_prompt: "Use $codex-chatgpt-dispatch to send verified context as direct text when it fits, create a handoff or freeze attachments only when needed, and submit exactly once before following my requested completion mode."

policy:
  products:
    - CODEX
  allow_implicit_invocation: false
