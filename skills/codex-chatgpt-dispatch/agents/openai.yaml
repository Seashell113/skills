interface:
  display_name: "Codex ChatGPT Dispatch"
  short_description: "Browser 调度 ChatGPT 文本与图片产物"
  default_prompt: "Use $codex-chatgpt-dispatch to prepare only the context this task needs, operate the built-in Browser on the correct ordinary ChatGPT web conversation for one authorized submission, wait without intervention, recover the text or image result from the same page, and verify it locally."

policy:
  products:
    - CODEX
  allow_implicit_invocation: false
